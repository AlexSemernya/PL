{
  "name": "lifeos-planner",
  "version": "1.0.0",
  "private": true,
  "description": "LifeOS — Telegram Mini App planner (static, no bundler).",
  "scripts": {
    "build": "mkdir -p build && cp index.html build/index.html",
    "start": "npx --yes serve build -l 3000"
  }
}
